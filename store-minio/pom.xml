<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>dev.infinityknowledge</groupId>
        <artifactId>infinity-knowledge-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>
    <artifactId>store-minio</artifactId>
    <name>Infinity Knowledge :: MinIO Store</name>

    <!-- Kept local until the parent dependency catalog owns all adapter versions. -->
    <properties>
        <minio.version>8.5.17</minio.version>
        <spotbugs-annotations.version>4.8.6</spotbugs-annotations.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>dev.infinityknowledge</groupId>
            <artifactId>knowledge-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.infinityknowledge</groupId>
            <artifactId>knowledge-spi</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.minio</groupId>
            <artifactId>minio</artifactId>
            <version>${minio.version}</version>
        </dependency>
        <!-- MinIO exposes SpotBugs annotations in its public signatures; javac -Werror needs them. -->
        <dependency>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-annotations</artifactId>
            <version>${spotbugs-annotations.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
