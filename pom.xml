<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>dev.infinityknowledge</groupId>
    <artifactId>infinity-knowledge-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Infinity Knowledge Runtime</name>
    <description>面向企业 Agent 的多租户知识运行时。</description>

    <modules>
        <module>knowledge-domain</module>
        <module>knowledge-spi</module>
        <module>knowledge-retrieval</module>
        <module>knowledge-runtime</module>
        <module>knowledge-agent-client</module>
        <module>knowledge-evaluation</module>
        <module>knowledge-ingestion</module>
        <module>parser-docling</module>
        <module>tokenizer-huggingface</module>
        <module>knowledge-compiler</module>
        <module>connector-obsidian</module>
        <module>provider-zhipu</module>
        <module>store-postgres</module>
        <module>store-milvus</module>
        <module>store-elasticsearch</module>
        <module>store-neo4j</module>
        <module>store-minio</module>
        <module>knowledge-jobs</module>
        <module>control-plane</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <testcontainers.version>2.0.5</testcontainers.version>
        <milvus.sdk.version>2.6.20</milvus.sdk.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>true</parameters>
                        <compilerArgs>
                            <arg>-Xlint:all</arg>
                            <arg>-Werror</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
