# 运行保障与跨地域切换

## OPS-FAILOVER-7

运行规程 `OPS-FAILOVER-7` 要求跨地域故障切换前完成三项检查：确认目标地域数据复制延迟
不超过 30 秒、冻结写入入口、由当班负责人复核切换清单。切换完成后必须恢复写入并执行
关键交易探测，不能只以负载均衡健康检查作为成功依据。

## 回退条件

如果目标地域关键交易探测连续两次失败，立即停止扩大流量并回退到原地域；同时保留故障窗口
的请求标识和稳定错误码用于复盘，不记录用户查询正文或访问令牌。
